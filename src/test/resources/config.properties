###########################
# Application URLs
###########################

# Login page endpoint
login.url=https://qeaskillhub.cognizant.com/LoginPage?logout=true

# Admin home/dashboard (optional; used after login if provided)

# Registration page (used by Create Account tests)
register.url=https://qeaskillhub.cognizant.com/pages_Register


###########################
# Credentials for UI Tests
###########################
login.username=admin_user
login.password=testadmin


# choose the one that actually hosts the 4 counters
# Correct landing after login
dashboard.url=https://qeaskillhub.cognizant.com/AdminDashboard

# Make sure this is empty (so it doesn?t override navigation)
status.url=
# or
###########################
# Element Locators (Login Page)
# Keep these if your LoginPage uses them dynamically.
###########################
login.username.id=txtUsername
login.password.id=txtPassword
login.button.id=BtnLogin


###########################
# Browser & CI Settings
###########################

# Headless mode (true/false). Your Base/Driver setup should read this.
headless=true

# Window size for headless mode (Chrome/CI)
window.width=1920
window.height=1080
